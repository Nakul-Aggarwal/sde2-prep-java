public class NumberOfNodesInTheSubTreeWithTheSameLabel {
    // TODO: Implement Number of nodes in the sub-tree with the same label
    // Problem: You are given a tree (i.e., a connected, undirected graph that has no cycles) consisting of n nodes numbered from 0 to n - 1 and exactly n - 1 edges. The root of the tree is the node 0, and each node has a label which is a lower-case character given in the string labels (i.e., the node with the number i has the label labels[i]).
    // The queries are given in an array queries, where queries[i] = [nodei, chari] means that we want to know the number of nodes in the subtree of nodei which have the same label that chari has.
    // Return an array answer of length queries.length, where answer[i] is the answer of the i-th query.

    // Pseudo code:
    // - DFS

    public int[] countSubTrees(int n, int[][] edges, String labels) {
        // TODO: Implement
        return new int[0];
    }
}