public class NumberOfGoodPaths {
    // TODO: Implement Number of good paths
    // Problem: There is a tree (i.e., a connected, undirected graph with no cycles) consisting of n nodes numbered from 0 to n - 1 and exactly n - 1 edges.
    // The nodes are colored from 0 to n - 1, and the colors are represented by the array vals, where vals[i] is the color of the ith node.
    // Two nodes u and v are said to be connected if there is a path between them in the tree.
    // A good path is a simple path where the starting node and the ending node have the same color, and the number of nodes with the same color as the starting node on the path is greater than or equal to the number of nodes with different colors on the path.
    // Return the number of good paths. Note that order does not matter, i.e., if (u, v) is considered a good path, then (v, u) is not considered a different path.

    // Pseudo code:
    // - DFS with union find or map

    public int numberOfGoodPaths(int[] vals, int[][] edges) {
        // TODO: Implement
        return 0;
    }
}