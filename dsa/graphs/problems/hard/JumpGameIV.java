public class JumpGameIV {
    // TODO: Implement Jump game IV
    // Problem: Given an array of integers arr, you are initially positioned at the first index of the array.
    // In one move you can jump from index i to index j (i != j):
    // - i and j are in the same parity group: i % 2 == j % 2
    // - or arr[i] == arr[j]
    // In other words, you can jump from i to j if i and j have the same parity (both even or both odd) or if arr[i] == arr[j].
    // Return the minimum number of moves to reach the last index of the array. It is guaranteed that you can reach the last index.

    // Pseudo code:
    // - BFS

    public int minJumps(int[] arr) {
        // TODO: Implement
        return 0;
    }
}