public class DetonateTheMaximumBombs {
    // TODO: Implement Detonate the maximum bombs
    // Problem: You are given a list of bombs. The range of a bomb is defined as the area where its effect can be felt. This area is in the shape of a circle with the center as the location of the bomb.
    // The bombs are represented by a 0-indexed 2D integer array bombs where bombs[i] = [x, y, r] denotes the position of the ith bomb at (x, y) and a radius r.
    // A bomb i detonates bomb j if:
    // The bomb j is within the radius of bomb i.
    // The bomb i has not detonated yet when bomb j does.
    // You need to find the maximum number of bombs that can detonate starting from one bomb.

    // Pseudo code:
    // - Build graph, DFS/BFS from each

    public int maximumDetonation(int[][] bombs) {
        // TODO: Implement
        return 0;
    }
}