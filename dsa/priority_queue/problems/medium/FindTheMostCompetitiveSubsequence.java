public class FindTheMostCompetitiveSubsequence {
    // TODO: Implement Find the most competitive subsequence
    // Problem: Given an integer array nums and a positive integer k, return the most competitive subsequence of nums of size k.
    // An array's subsequence is obtained by deleting some number of elements (can be zero) from the array, leaving the remaining elements in their original order.
    // A subsequence a is more competitive than a subsequence b (of the same length) if in the first position where a and b differ, subsequence a has a smaller value than b. For example, [1,3,4] is more competitive than [1,3,5] because the first position they differ is at the final element, and 4 < 5.

    // Pseudo code:
    // - Stack

    public int[] mostCompetitive(int[] nums, int k) {
        // TODO: Implement
        return new int[0];
    }
}