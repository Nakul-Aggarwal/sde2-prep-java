public class KthSmallestPrimeFraction {
    // TODO: Implement Kth smallest prime fraction
    // Problem: You are given a sorted integer array arr containing 1 and prime numbers, where all the integers of arr are unique. You are also given an integer k.
    // For every i < j, [arr[i], arr[j]] is a valid fraction.
    // We can sort these fractions in increasing order of their values.
    // Return the kth smallest fraction considered. Return your answer as an array of integers of size 2, where answer[0] == arr[i] and answer[1] == arr[j].

    // Pseudo code:
    // - Min heap

    public int[] kthSmallestPrimeFraction(int[] arr, int k) {
        // TODO: Implement
        return new int[0];
    }
}