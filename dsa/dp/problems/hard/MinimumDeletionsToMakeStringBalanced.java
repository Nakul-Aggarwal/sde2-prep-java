public class MinimumDeletionsToMakeStringBalanced {
    // TODO: Implement Minimum deletions to make string balanced
    // Problem: You are given a string s consisting only of characters 'a' and 'b'.
    // You can delete any number of characters in s to make s balanced. When you delete a character, all characters to the right of it shift left.
    // s is balanced if there is no pair of indices (i,j) such that i < j and s[i] = 'b' and s[j] = 'a'.
    // Return the minimum number of deletions needed to make s balanced.

    // Pseudo code:
    // - DP: Count a's after b's

    public int minimumDeletions(String s) {
        // TODO: Implement
        return 0;
    }
}